1、RISC-V的函数调用过程参数优先使用寄存器a0~a7传递，共8个寄存器。printf的参数13保存在a2寄存器。

2、从call.asm可以看出，这两个都被内联优化处理了。main中的f调用直接使用了结果12，而f中的函数g调用直接内联在f中了。

3、0x630

4、为0x38，为函数的返回地址

5、输出为He110 World，如果是大端法，应该将输入改为0x726c64

6、这里y的值取决于执行这条语句前寄存器a2的值。
